# Request journey report
Request: Build the "home" page from Figma frame "Corporate Product Showcase Website" (RPC file, node 1:2)
Date: 2026-08-03

## Spec
- Generated / reused from cache: Generated fresh (no prior cache existed). Written to `/design/spec-shared.md` and `/design/spec-home.md`. Assets exported to `/src/assets/` (6 photos, 1 logo, 20 icon SVGs); screenshot reference saved to `/design/reference/home-screenshot.png`.
- Pages affected: home
- Open questions raised: None blocking. Several assumptions were flagged inline in the spec's "Open questions" sections with reasonable defaults per the template's own design (no tablet/mobile frames provided; some icon/photo-to-slot mappings resolved by visual match rather than deterministic metadata; a few section background colors not exposed via Figma Variables were resolved against the screenshot).

## Build
- Sections rebuilt: All sections (fresh build) — top utility bar, main nav, hero carousel (3 slides + dots), intro section, products grid (6 cards), interior solutions, testimonials, trusted-by-organizations strip, CTA banner, newsletter strip, footer.
- Sections reused from cache: None (first build for this page).

## QA
- Rounds run: 3
  - Round 1: FAIL — logo.png was a broken/placeholder asset in header and footer. Root cause: initial export grabbed the wrong image; orchestrator re-exported the correct icon-mark asset directly from Figma node 1:484.
  - Round 2: PASS (scoped re-verification of the logo fix; coding-agent needed no code changes, only the asset swap).
  - Round 3: PASS (scoped confirmation of a hero-carousel layout fix found during final review — see below).
- Final result: pass
- Escalation reason (if any): None — all issues resolved within the round cap (3).

## Final review
- Method used: Visual diff (headless-browser screenshot of the built page vs. the Figma reference screenshot) + review of the QA report, per `/guidelines/global.md`.
- Result: Initially caught one defect QA had missed — a ~376px blank white gap between the hero carousel and the intro section, caused by the active hero slide switching to `position: relative` with no explicit height (collapsing to ~224px) while inactive slides remained `position: absolute; inset: 0` (correctly stretching to the parent's 600px height). Fixed by keeping all slides `position: absolute` at all times and toggling active/inactive purely via opacity/visibility/z-index. Re-verified via a fresh headless-browser render: gap eliminated, layout now matches the Figma reference. Confirmed via a follow-up QA round (round 3, passed). Final review passes.

Notes for future runs:
- The `markdown-agent` and `qa-agent` subagent definitions (`.claude/agents/*.md`) had stale Figma MCP tool names (`get_code`, `get_image`) left over from an older Figma MCP server version; both were updated to the current tool set (`get_design_context`, `get_screenshot`, `get_metadata`, `get_variable_defs`, `download_assets`). Despite the fix, subagents in this session were still unable to actually invoke the newly-granted Figma MCP tools (a platform-level limitation observed this run, not a config issue) — the orchestrator worked around this by pulling Figma data and doing the Figma-dependent QA/final-review comparisons directly instead of through the subagents.
